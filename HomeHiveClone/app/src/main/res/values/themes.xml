<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.HomeHiveClone" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">@color/splash_screen_bg</item>
        <item name="android:windowLightStatusBar">true</item> <!-- for android 5.0 >= -->
    </style>

    <style name="Theme.HomeHiveClone" parent="Base.Theme.HomeHiveClone" />
</resources>